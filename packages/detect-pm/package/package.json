{
  "name": "@lerepo/detect-pm",
  "version": "0.2.0",
  "publishConfig": {
    "access": "public"
  },
  "description": "Library to detect which package manager is being used.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/lerepo/workspace-tools.git"
  },
  "main": "./lib/index.js",
  "engines": {
    "node": ">=12.18.3"
  },
  "scripts": {
    "_r:clean": "rimraf _release && rimraf _release.tgz",
    "_r:prepare": "yarn pack --out _release.tgz && jaguar -x _release.tgz && rimraf _release.tgz",
    "babel:build": "babel --root-mode upward src --out-dir lib --extensions '.ts' --env-name production",
    "babel:watch": "babel --root-mode upward src --out-dir lib --extensions '.ts' --watch",
    "build": "yarn babel:build --quiet && yarn tsc",
    "build:quiet": "yarn babel:build --quiet",
    "clean": "rimraf dist build lib",
    "format": "yarn g:format",
    "format:all": "yarn g:format:all",
    "format:list": "yarn g:format:list",
    "lint": "yarn g:lint",
    "lint:fix": "run-lint --fix",
    "lint:staged": "yarn g:lint:staged",
    "prepack": "yarn clean && yarn build",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch",
    "tsc": "tsc --emitDeclarationOnly -p tsconfig.lib.json",
    "tsc:check": "tsc --noEmit -p tsconfig.json",
    "tsc:watch": "yarn run tsc:check --watch"
  },
  "dependencies": {
    "lodash": "^4.17.21",
    "regenerator-runtime": "^0.13.9",
    "semver": "^7.3.7"
  },
  "devDependencies": {
    "@babel/cli": "7.18.10",
    "@babel/core": "^7.19.1",
    "@babel/plugin-proposal-class-properties": "^7.18.6",
    "@babel/plugin-proposal-decorators": "^7.19.1",
    "@babel/plugin-proposal-object-rest-spread": "^7.18.9",
    "@babel/preset-env": "^7.19.1",
    "@babel/preset-typescript": "^7.18.6",
    "@types/jest": "^29.0.3",
    "@types/node": "^16",
    "@types/semver": "^7.3.12",
    "babel-plugin-import": "^1.13.5",
    "babel-plugin-module-resolver": "^4.1.0",
    "babel-plugin-transform-define": "^2.1.0",
    "core-js": "^3.25.2",
    "jaguar": "^6.0.1",
    "jest": "^29.0.3",
    "prettier": "^2.7.1",
    "prettier-plugin-package": "^1.3.0",
    "rimraf": "^3.0.2",
    "typescript": "4.8.3"
  }
}